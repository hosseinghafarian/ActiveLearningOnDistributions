function [rbf_gamma_is] = comp_best_gamma_mmd(data, gamma_list)
% we search for the value of gamma which maximizes the value of the 
best_gamma_val = -Inf; 
for gamma_is = gamma_list
    
    
end

end